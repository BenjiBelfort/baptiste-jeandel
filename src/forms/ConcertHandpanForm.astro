---
/** Sous-formulaire : Concert Handpan */
---
<section
  data-domain-form="Concert Handpan"
  class="hidden border border-stone-900 bg-red-100 p-4 md:p-5 shadow-md shadow-red-300/50"
  aria-label="Questions spécifiques — Concert Handpan"
>
  <h2 class="hand text-3xl mb-6 text-center">Concert Handpan</h2>

  <!-- Téléphone -->
  <div class="grid md:grid-cols-2 gap-4">
    <label class="block">
      <span class="text-sm">N° de téléphone *</span>
      <input
        data-reg-id="reg-tel"
        type="tel"
        required aria-required="true"
        class="mt-1 w-full border bg-white p-2"
        placeholder="03… / 06…"
        inputmode="tel"
        autocomplete="tel"
      />
    </label>
  </div>

  <!-- Nombre de personnes -->
  <label class="block mt-4">
    <span class="text-sm">Nombre de personnes participant(e)s *</span>
    <input
      data-reg-id="reg-nbParticipants"
      type="number" min="1" step="1"
      required aria-required="true"
      class="mt-1 w-full border bg-white p-2"
      placeholder="Ex. 100"
      inputmode="numeric"
    />
  </label>

  <!-- Type d’événement (optionnel) -->
  <div class="grid md:grid-cols-2 gap-4 mt-4">
    <label class="block">
      <span class="text-sm">De quel événement s’agit-il ?</span>
      <select
        data-reg-id="reg-typeEvent"
        class="mt-1 w-full border bg-white p-2"
      >
        <option value="">— Sélectionner —</option>
        <option value="mariage">Mariage</option>
        <option value="enterrement">Enterrement / Funérailles</option>
        <option value="anniversaire">Anniversaire</option>
        <option value="autre">Autre</option>
      </select>
    </label>

    <label class="block" data-show-if="reg-typeEvent" data-show-eq="autre">
      <span class="text-sm">Précisez l’événement</span>
      <input
        data-reg-id="reg-typeEventAutre"
        class="mt-1 w-full border bg-white p-2"
        placeholder="Ex. cérémonie laïque, inauguration…"
      />
    </label>
  </div>

  <!-- Adresse précise du lieu -->
  <label class="block mt-4">
    <span class="text-sm">Adresse exacte du lieu où le concert aura lieu *</span>
    <textarea
      rows="3"
      data-reg-id="reg-lieuConcert"
      required aria-required="true"
      class="mt-1 w-full border bg-white p-2"
      placeholder="Nom du lieu, rue, code postal, ville…"
      autocomplete="street-address"
    ></textarea>
  </label>

  <!-- Date + Horaire -->
  <div class="grid md:grid-cols-2 gap-4 mt-4">
    <label class="block">
      <span class="text-sm">Date de votre événement *</span>
      <input
        data-reg-id="reg-dateEvent"
        type="date"
        required aria-required="true"
        class="mt-1 w-full border bg-white p-2"
      />
    </label>

    <label class="block">
      <span class="text-sm">Horaire *</span>
      <input
        data-reg-id="reg-horaire"
        type="text"
        required aria-required="true"
        class="mt-1 w-full border bg-white p-2"
        placeholder="Ex. 18h30–19h / installation 17h30"
      />
    </label>
  </div>

  <!-- 🔒 Registre hidden -->
  <div data-registry class="hidden" aria-hidden="true">
    <input type="hidden" name="Téléphone" id="reg-tel" disabled />
    <input type="hidden" name="Nombre de personnes" id="reg-nbParticipants" disabled />

    <input type="hidden" name="Type d’événement" id="reg-typeEvent" disabled />
    <input type="hidden" name="Événement (précision)" id="reg-typeEventAutre" disabled />

    <input type="hidden" name="Adresse (lieu du concert)" id="reg-lieuConcert" disabled />

    <input type="hidden" name="Date de l’événement" id="reg-dateEvent" disabled />
    <input type="hidden" name="Horaire" id="reg-horaire" disabled />
  </div>
</section>
