---
/** Sous-formulaire : Créer et Mener un atelier Musical EHPAD (Formation)
 * - Champs visibles avec data-reg-id="<id du hidden>"
 * - Registre hidden (names Netlify) désactivé par défaut
 */
---

<section
  data-domain-form="Atelier EHPAD"
  class="hidden border border-stone-900 bg-green-100 p-4 md:p-5"
  aria-label="Questions spécifiques — Formation Atelier EHPAD"
>
  <h2 class="hand text-3xl mb-6 text-center">Créer et mener un atelier musical — EHPAD</h2>

  <!-- Coordonnées -->
  <div class="grid md:grid-cols-2 gap-4">
    <label class="block">
      <span class="text-sm">N° de téléphone *</span>
      <input data-reg-id="reg-tel" type="tel" required class="mt-1 w-full border bg-white p-2" placeholder="03… / 06…" inputmode="tel" />
    </label>
  </div>

  <label class="block mt-4">
    <span class="text-sm">Structure et adresse *</span>
    <textarea rows="3" data-reg-id="reg-structure" required class="mt-1 w-full border bg-white p-2" placeholder="Nom de la structure et adresse"></textarea>
  </label>

  <div class="grid md:grid-cols-2 gap-4 mt-4">
    <label class="block">
      <span class="text-sm">Code postal *</span>
      <input data-reg-id="reg-cp" required class="mt-1 w-full border bg-white p-2" placeholder="90000" inputmode="numeric" />
    </label>

    <label class="block">
      <span class="text-sm">Ville *</span>
      <input data-reg-id="reg-ville" required class="mt-1 w-full border bg-white p-2" />
    </label>
  </div>

  <!-- Participants & profil -->

    <label class="block mt-4">
        <span class="text-sm">Combien de personnes sont intéressées par la formation ? *</span>
        <input data-reg-id="reg-nbPersonnes" type="number" min="1" step="1" required class="mt-1 w-full border bg-white p-2" placeholder="ex : 6" inputmode="numeric" />
    </label>

    <label class="block mt-4">
        <span class="text-sm">Quel est leur profil ? (type de poste, missions) *</span>
        <input data-reg-id="reg-profil" required class="mt-1 w-full border bg-white p-2" placeholder="AS, AMP, IDE, animateur·rice, psychologue…" />
    </label>


  <!-- Matériel -->
  <label class="block mt-4">
    <span class="text-sm">Avez-vous déjà du matériel sur place ? Si oui, précisez</span>
    <textarea rows="3" data-reg-id="reg-matos" class="mt-1 w-full border bg-white p-2" placeholder="Listez le matériel que vous avez…"></textarea>
  </label>

  <!-- Public des résidents -->
  <fieldset class="mt-6">
    <legend class="text-sm font-medium">Les personnes qui vont se former travaillent surtout avec des résident·e·s :</legend>
    <div class="mt-2 grid sm:grid-cols-2 gap-2">
      <label class="inline-flex items-center gap-2">
        <input type="checkbox" data-reg-id="reg-publicCible" value="troubles tres avances" class="border" />
        <span class="text-sm">avec troubles cognitifs très avancés</span>
      </label>
      <label class="inline-flex items-center gap-2">
        <input type="checkbox" data-reg-id="reg-publicCible" value="troubles avances" class="border" />
        <span class="text-sm">avec troubles cognitifs avancés</span>
      </label>
      <label class="inline-flex items-center gap-2">
        <input type="checkbox" data-reg-id="reg-publicCible" value="troubles moderes" class="border" />
        <span class="text-sm">avec troubles cognitifs modérés</span>
      </label>
      <label class="inline-flex items-center gap-2">
        <input type="checkbox" data-reg-id="reg-publicCible" value="sans troubles" class="border" />
        <span class="text-sm">sans troubles cognitifs</span>
      </label>
    </div>
  </fieldset>

  <!-- Niveau de dépendance GIR -->
  <div class="grid md:grid-cols-2 gap-4 mt-6">
    <label class="block">
      <span class="text-sm">Niveau de dépendance des résident·e·s *</span>
      <select data-reg-id="reg-gir" required class="mt-1 w-full border bg-white p-2">
        <option value="">— Sélectionner —</option>
        <option value="GIR 1-2">GIR 1 - 2</option>
        <option value="GIR 3-4">GIR 3 - 4</option>
      </select>
    </label>

    <!-- Modalité de travail -->
    <label class="block">
      <span class="text-sm">Modalité de travail souhaitée *</span>
      <select data-reg-id="reg-modalite" required class="mt-1 w-full border bg-white p-2">
        <option value="">— Sélectionner —</option>
        <option value="groupe">En groupe</option>
        <option value="individuel">En individuel</option>
        <option value="les-deux">Les deux</option>
      </select>
    </label>
  </div>

  <!-- 🔒 Registre hidden de ce sous-form -->
  <div data-registry class="hidden" aria-hidden="true">
    <input type="hidden" name="Téléphone" id="reg-tel" disabled />
    <input type="hidden" name="Structure (adresse)" id="reg-structure" disabled />
    <input type="hidden" name="Code postal" id="reg-cp" disabled />
    <input type="hidden" name="Ville" id="reg-ville" disabled />
    <input type="hidden" name="Nombre de personnes intéressées" id="reg-nbPersonnes" disabled />
    <input type="hidden" name="Profil des apprenant(e)s" id="reg-profil" disabled />
    <input type="hidden" name="Matériel sur place (précisions)" id="reg-matos" disabled />
    <input type="hidden" name="Public ciblé (résident·e·s)" id="reg-publicCible" disabled />
    <input type="hidden" name="Niveau de dépendance (GIR)" id="reg-gir" disabled />
    <input type="hidden" name="Modalité de travail souhaitée" id="reg-modalite" disabled />
  </div>
</section>
