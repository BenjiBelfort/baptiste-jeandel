---
/** Sous-formulaire : Éveil musical */
---
<section
  data-domain-form="Éveil Musical"
  class="hidden border border-stone-900 bg-red-100 p-4 md:p-5 shadow-md shadow-red-300/50"
  aria-label="Questions spécifiques — Éveil musical"
>
  <h2 class="hand text-3xl mb-6 text-center">Éveil Musical</h2>

  <!-- Téléphone -->
  <div class="grid md:grid-cols-2 gap-4 mt-4">
    <label class="block">
      <span class="text-sm">N° de téléphone *</span>
      <input
        data-reg-id="reg-tel"
        type="tel"
        required
        aria-required="true"
        class="mt-1 w-full border bg-white p-2"
        placeholder="03… / 06…"
        inputmode="tel"
        autocomplete="tel"
      />
    </label>
  </div>

  <!-- Structure & adresse -->
  <label class="block mt-4">
    <span class="text-sm">Structure et adresse *</span>
    <textarea
      rows="3"
      data-reg-id="reg-structure"
      required
      aria-required="true"
      class="mt-1 w-full border bg-white p-2"
      placeholder="Nom et adresse de la structure"
      autocomplete="organization"
    ></textarea>
  </label>

  <!-- CP + Ville -->
  <div class="grid md:grid-cols-2 gap-4 mt-4">
    <label class="block">
      <span class="text-sm">Code postal *</span>
      <input
        data-reg-id="reg-cp"
        required
        aria-required="true"
        class="mt-1 w-full border bg-white p-2"
        placeholder="90000"
        inputmode="numeric"
        pattern="\\d{5}"
        title="5 chiffres (code postal en France)"
        autocomplete="postal-code"
      />
    </label>

    <label class="block">
      <span class="text-sm">Ville *</span>
      <input
        data-reg-id="reg-ville"
        required
        aria-required="true"
        class="mt-1 w-full border bg-white p-2"
        autocomplete="address-level2"
        placeholder="Ex. Belfort"
      />
    </label>
  </div>

  <!-- Type de mise en place -->
  <label class="block mt-4">
    <span class="text-sm">Vous souhaitez mettre en place… *</span>
    <select
      data-reg-id="reg-typeEveil"
      required
      aria-required="true"
      class="mt-1 w-full border bg-white p-2"
    >
      <option value="">— Sélectionner —</option>
      <option value="regulieres">Des séances régulières</option>
      <option value="spectacle">Un spectacle</option>
      <option value="parents-enfants">Des séances parents–enfants</option>
    </select>
  </label>

  <!-- Fréquence -->
  <div class="grid md:grid-cols-2 gap-4 mt-4">
    <label class="block">
      <span class="text-sm">Fréquence souhaitée *</span>
      <select
        data-reg-id="reg-frequence"
        required
        aria-required="true"
        class="mt-1 w-full border bg-white p-2"
      >
        <option value="">— Sélectionner —</option>
        <option value="hebdo">Hebdo</option>
        <option value="quinzaine">15 jours</option>
        <option value="mensuel">Mensuel</option>
        <option value="autre">Autre</option>
      </select>
    </label>

    <label class="block" data-show-if="reg-frequence" data-show-eq="autre">
      <span class="text-sm">Précisez la fréquence *</span>
      <input
        data-reg-id="frequenceAutre"
        data-required
        class="mt-1 w-full border bg-white p-2"
        placeholder="Ex. toutes les 3 semaines"
      />
    </label>
  </div>

  <!-- Infos atelier -->
  <div class="grid md:grid-cols-2 gap-4 mt-4">
    <label class="block">
      <span class="text-sm">Âge des enfants *</span>
      <input
        data-reg-id="reg-age"
        required
        aria-required="true"
        class="mt-1 w-full border bg-white p-2"
        placeholder="Ex. 2–3 ans"
      />
    </label>

    <label class="block">
      <span class="text-sm">Nombre de participants *</span>
      <input
        data-reg-id="reg-nbParticipants"
        type="number"
        min="1"
        required
        aria-required="true"
        class="mt-1 w-full border bg-white p-2"
        placeholder="min 1"
      />
    </label>

    <label class="block md:col-span-2">
      <span class="text-sm">Lieu</span>
      <textarea
        data-reg-id="reg-lieu"
        class="mt-1 w-full border bg-white p-2"
        placeholder="Adresse, salle, contraintes d’accès…"
      ></textarea>
    </label>

    <label class="block md:col-span-2">
      <span class="text-sm">Date souhaitée, à partir du :</span>
      <input
        data-reg-id="reg-dateSouhaitee"
        type="date"
        class="mt-1 w-full border bg-white p-2"
      />
    </label>
  </div>

  <!-- 🔒 Registre hidden (NOMS EXACTS pour le webhook) -->
  <div data-registry class="hidden" aria-hidden="true">
    <input type="hidden" name="Téléphone" id="reg-tel" disabled />
    <input type="hidden" name="Structure (adresse)" id="reg-structure" disabled />
    <input type="hidden" name="Code postal" id="reg-cp" disabled />
    <input type="hidden" name="Ville" id="reg-ville" disabled />
    <input type="hidden" name="Type de demande (éveil)" id="reg-typeEveil" disabled />
    <input type="hidden" name="Fréquence (éveil)" id="reg-frequence" disabled />
    <input type="hidden" name="Fréquence (précision)" id="frequenceAutre" disabled />
    <input type="hidden" name="Âge des enfants" id="reg-age" disabled />
    <input type="hidden" name="Nombre de participants" id="reg-nbParticipants" disabled />
    <input type="hidden" name="Lieu" id="reg-lieu" disabled />
    <input type="hidden" name="Date souhaitée" id="reg-dateSouhaitee" disabled />
  </div>
</section>
